export interface FormProps {
    value: string; // Define the type for 'value'
    updateValue: (newValue: string) => void; // Define the type for 'updateValue' as a function that takes a string parameter and returns void
  }